server:
  type: simple
  connector:
    type: http
    port: 8080

database:
    # the name of the JDBC driver, mysql in our case
    driverClass: com.mysql.jdbc.Driver
    # the username
    user: razi
    # the password
    password: access@4477
    # the JDBC URL; the database is called DWGettingStarted
    url: jdbc:mysql://localhost:3306/CrudApis